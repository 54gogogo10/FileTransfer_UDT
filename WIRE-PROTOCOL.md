# TrFileTransfer — 线协议规范

## TCP 协议

单文件和文件夹传输都以单个类型字节开始。

### 单文件（类型 `0x00`）

```
偏移  大小  字段
----  ----  -----
0     1     类型 = 0x00
1     8     文件大小，Int64 小端序
9     4     文件名长度（字节），Int32 小端序
13    N     文件名，UTF-8
13+N  M     文件内容（fileSize 字节）
13+N+M 32  文件内容 SHA-256 哈希
```

### 文件夹（类型 `0x01`）

```
偏移  大小  字段
----  ----  -----
0     1     类型 = 0x01
1     2     文件夹名长度（字节），Int16 小端序
3     N     文件夹名，UTF-8
3+N   4     文件数量，Int32 小端序
```

紧接着是每个文件的条目：

```
偏移  大小  字段
----  ----  -----
0     8     文件大小，Int64 小端序
8     2     相对路径长度（字节），Int16 小端序
10    N     相对路径，UTF-8（如 "subdir/file.txt"）
10+N  M     文件内容（fileSize 字节）
10+N+M 32  此文件内容 SHA-256 哈希
```

所有多字节整数均为小端序。

### 分块文件（类型 `0x02`）

用于并发传输——将单个大文件拆分为多个等大分块，每个分块通过独立连接并行发送。服务端通过 `ChunkTracker` 按文件名聚合，写入预分配文件（`SetLength`）的对应偏移位置。

```
[1 byte:  0x02]                // 分块文件标记
[8 bytes: Int64 totalFileSize] // 文件总大小（所有分块合计）
[8 bytes: Int64 chunkOffset]   // 此分块在文件中的字节偏移
[8 bytes: Int64 chunkSize]     // 此分块数据大小
[4 bytes: Int32 nameLen]       // 文件名长度
[N bytes: UTF-8 fileName]      // 文件名（所有分块使用相同文件名，作为聚合键）
[M bytes: chunkData]           // chunkSize 字节的数据
[32 bytes: SHA256]             // 此分块数据的 SHA-256
```

每个分块独立发送头部+数据+哈希。服务端收到首个分块时创建 `ChunkTracker`：
- 调用 `Utils.GetUniqueSavePath` 解析保存路径（处理名称冲突）
- 创建 `FileStream`，调用 `SetLength(totalSize)` 预分配
- 后续分块 Seek 到对应偏移写入，`BytesReceived` 累加
- `BytesReceived >= TotalSize` 时文件完成，触发 `OnTransferComplete`，释放 tracker

末块可能小于其他块——服务端以头部 `chunkSize` 为准，不自行计算。

### 断点续传（类型 `0x03` 与 `0x04`）

单文件续传使用 `0x03`（头部 + 0x10 协商响应的完整字节布局见 AGENTS.md "TCP 线协议" 章节）；文件夹续传使用 `0x04`，两者共享同一偏移协商思想：**服务器偏移权威**，服务器无状态时客户端声明偏移不被信任、从头发送。

#### 文件夹续传（类型 `0x04`）

客户端发送清单（manifest），服务器扫描磁盘上已收到的文件后回答续传点：

```
[1 byte:  0x04]                 // 文件夹续传标记
[16 bytes: sessionId]           // 会话 GUID
[2 bytes: Int16 folderNameLen]
[N bytes: UTF-8 folderName]
[4 bytes: Int32 fileCount]
[8 bytes: Int64 totalBytes]     // 所有文件大小之和（必须等于清单合计，否则拒绝）
每个文件（清单，按顺序）：
    [8 bytes: Int64 fileSize]
    [2 bytes: Int16 pathLen]
    [N bytes: UTF-8 relativePath]
    [32 bytes: SHA256]          // 该文件完整内容的哈希（供服务器校验已收文件）
```

服务器协商响应（`0x11`，26 字节）：

```
[1 byte:  0x11]
[8 bytes: Int64 resumeFileIndex]  // 首个未完成文件的清单序号（全部完成 = fileCount）
[8 bytes: Int64 resumeOffset]     // 该文件内已收到的字节偏移
[1 byte:  status]                 // 0=新会话 1=续传中 2=已全部完成
```

随后客户端从 `(resumeFileIndex, resumeOffset)` 开始，按清单顺序发送剩余内容，每个文件沿用 0x01 的文件体格式：

```
[M bytes: fileData]             // 该文件本次实际发送的字节（续传文件从 resumeOffset 起）
[32 bytes: SHA256]              // 本次发送字节的 SHA-256（增量哈希，同 0x03）
```

服务器行为：
- 保存目录由会话确定性推导：`<saveDir>/<folderName>.<sessionId 前 8 位>`——同一会话跨重启映射到同一目录，无需服务器落盘状态（磁盘文件本身就是状态）。
- 续传扫描：文件存在且大小相符 → 用清单哈希全文件校验，通过则跳过，不符则从 0 重写；文件存在且偏小 → 从其大小偏移续传；其余（缺失/为空/超长）→ 从 0 重写。
- 假定清单顺序发送（与 0x01 一致的顺序遍历）；任一文件增量哈希不符即中止，保留部分文件供下次续传。

### 配对码认证（类型 `0x05`，可选前导帧）

服务器开启"配对码"后，客户端在任何传输类型之前必须先发认证帧；未开启时客户端也可自愿发送（服务器宽容接受）。

```
[1 byte:  0x05]
[32 bytes: SHA256(UTF-8 配对码)]   // 原始配对码不跨网络传输
```

服务器响应（`0x15`，2 字节）：

```
[1 byte:  0x15]
[1 byte:  status]               // 0=通过 1=拒绝（随后服务器关闭连接）
```

服务器行为：
- 启用配对码：哈希与 `SHA256(当前配对码)` 恒时比较，不符回 status=1 并断开；若客户端直接发送传输类型（未认证），同样回 status=1 后拒绝。
- 未启用：任何 0x05 帧回 status=0（宽容路径，客户端可在两种服务器间无差别工作）。
- 配对码由服务器每次启动会话时随机生成（6 位数字），仅显示在本机界面，不入配置文件。
- 拒绝路径（IP 过滤 / 配对码不符 / 接收确认拒绝）在 TCP 上以 `SO_LINGER(0)` 立即 RST 关闭，让发送方尽快失败；UDT 上回 1 字节 `0x00` NACK（区别于成功 ACK `0x01`）。

### 加密认证 + 密钥协商（类型 `0x09`，可选前导帧，v2.12+）

客户端填有配对码且开启"加密传输"时，用 0x09 取代 0x05：一次完成 ECDH 密钥协商 + 双方身份确认 + 配对码校验，成功后整个会话（头部、文件内容、哈希、文本——全部字节）经加密流传输。

**为什么不能像 v2.9 那样直接用配对码哈希当密钥**：配对码只有 6 位十进制，其 SHA256 若跨网络传输，任何抓到握手的被动窃听者都能直接算出会话密钥——加密形同虚设。0x09 改为：会话密钥来自**临时 ECDH 共享秘密**，配对码只用于对握手做认证（HMAC 确认），网线上不出现任何可推出密钥的材料。

```
[1 byte:  0x09]
[32 bytes: SHA256(UTF-8 配对码)]        // 仅用于服务器校验，不作为密钥
[72 bytes: 客户端临时公钥 P-256]        // BCRYPT ECCPUBLICBLOB（little-endian x/y）
```

服务器响应（`0x19`，固定 106 字节，明文）：

```
[1 byte:  0x19]
[1 byte:  status]               // 0=通过 1=配对码错误(拒) 2=本机未启用配对码(继续明文)
[72 bytes: 服务器临时公钥 P-256]
[32 bytes: confirmS]            // HMAC-SHA256(authKey, "s2c-confirm" ‖ 双方公钥)
```

客户端确认（32 字节，仅 status=0 时发送）：

```
[32 bytes: confirmC]            // HMAC-SHA256(authKey, "c2s-confirm" ‖ 双方公钥)
```

密钥派生（两端一致）：

```
Z        = ECDH-P256(私钥, 对端公钥)                       // 32 字节共享秘密
salt     = SHA256(clientPub ‖ serverPub)                  // 绑定本次握手，防重放
psk      = PBKDF2-HMAC-SHA1(配对码, salt, 50000 次, 32)    // 提高离线爆破成本
authKey  = SHA256("tf-ecdh-auth" ‖ Z ‖ psk)
C2S_ENC  = SHA256(authKey ‖ salt=0 ‖ "c2s-enc")           // AES-256-CTR 密钥
C2S_MAC / S2C_ENC / S2C_MAC                              // 同理，用途字符串区分
```

- `confirmS`/`confirmC` 是双向密钥确认：只有同时掌握配对码与 ECDH 私钥的一方才算得出。任何一侧校验失败即断开——主动中间人无法在不猜出配对码的前提下完成握手。
- 被动窃听者拿到公钥与确认值也无法算出 `Z`（ECDH 难题），因此无法解密。
- 主动中间人只能离线爆破 6 位配对码（受 PBKDF2 拖慢）；**把配对码加长到 12 位以上即可使该攻击不现实**。
- 旧版本服务器不认识 0x09（当作 0x00 头解析必然校验失败并断开）。默认情况下新客户端据此**回退为明文 0x05 重连一次**（保持与旧版互操作）；把配置 `EncryptStrict=1` 设为严格模式则**拒绝回退**，以防中间人强制降级。

### 加密认证（类型 `0x07`，旧版兼容，v2.9+）

**仅用于与本程序 ≤2.11 版本互通**：那类旧服务器只认识 0x07。注意其会话密钥直接来自 0x07 帧中明文传输的 `SHA256(配对码)`，因此只能防"没看到握手的被动窃听者"，**不具备真正的机密性**。收到 0x07 时服务器会在日志中记录弱加密警告。

```
[1 byte:  0x07]
[32 bytes: SHA256(UTF-8 配对码)]   // 与 0x05 相同的预共享码哈希（旧版即以其为 PSK）
[16 bytes: sessionSalt]            // 客户端随机数（加密会话盐）
```

服务器响应（`0x17`，2 字节，明文）：

```
[1 byte:  0x17]
[1 byte:  status]               // 0=通过，后续加密 1=配对码错误（拒绝并断开）
                                // 2=本机未启用配对码，双方在同一连接上继续明文
```

密钥派生（仅旧版兼容路径，纯 SHA256 级联）：

```
master  = SHA256(salt ‖ psk)                    // psk = SHA256(配对码) —— 跨网络传输，不安全
C2S_ENC = SHA256(master ‖ "c2s-enc")            // 客户端→服务器 加密密钥（AES-256-CTR）
C2S_MAC = SHA256(master ‖ "c2s-mac")            // 客户端→服务器 MAC 密钥（HMAC-SHA256）
S2C_ENC / S2C_MAC                        = SHA256(master ‖ "s2c-enc" / "s2c-mac")
```

加密流（`EncryptedWireStream`，每个方向独立）：上层字节切成 ≤256 KB 的段。**0x09 会话**（v2.12+）每段带递增序号并被 MAC 覆盖，因此等长段被重排/交换会立即校验失败：

```
[8 bytes: Int64 seq（每方向从 0 递增，小端）]
[4 bytes: Int32 plainLen（小端）]
[16 bytes: 截断的 HMAC-SHA256(macKey, seq ‖ lenBytes ‖ ciphertext)]
[ciphertext]                    // AES-256-CTR，计数器从 0 起（密钥按会话+方向唯一，无 IV 复用）
```

**旧 0x07 会话**（≤2.11 兼容）沿用无序号的格式（`[4B len][16B MAC(len‖ciphertext)][密文]`），读取端按握手选择的格式解析——这是线格式差异，两端由是否走 `0x09` 决定。

- 写方向就地加密调用方缓冲（发送管线写入后不复用缓冲内容）；读方向在独立缓冲内解密。读写两方向可并发（发送管线的预读依赖此点），各自状态完全隔离。
- MAC 不符即抛异常断开（防篡改/防错配对）。
- 密钥仅存在于两端内存；配对码本身不落盘（服务器每次会话随机生成）。
- 并发分块传输（0x02）每条连接独立走一遍 0x07 握手，各自独立密钥。
- UDT 的 1 字节应用层 ACK 不在加密流内（收发双方都在原始套接字上读写）。

### 压缩协商（类型 `0x08`，可选前导帧，v2.11+）

客户端开启"传输压缩"时，在认证**之后**发送 0x08 帧：压缩装饰器包在最外层，因此线上字节为 `encrypt(compress(plain))`——deflate 看到的是明文而非密文，加密数据仍能被压缩。

```
[1 byte:  0x08]
[16 bytes: 随机填充]            // 旧版本服务器把它当作损坏的 0x00 头拒绝并断开
```

服务器响应（`0x18`，2 字节）：

```
[1 byte:  0x18]
[1 byte:  status]               // 0=接受，后续字节走压缩段；其他值=拒绝（继续未压缩）
```

压缩流（`CompressedWireStream`）：每次 `WriteExactAsync` 调用成为一个自包含段：

```
[4 bytes: UInt32 lenAndFlag（小端）]   // 最高位=1 表示 stored（未压缩）
[len 字节: deflate 载荷，或原始字节]
```

- 段长度上限 64 MB（解压炸弹防护）；长度 0 或超限一律抛 IOException 断开。
- 载荷小于 64 字节、或 deflate 后反而变大（随机数据、已压缩媒体）→ 走 stored 分支（原始字节 + 高位标记），不会膨胀。
- 读侧把段解压进待消费缓冲，`ReadExact`/`ReadSome` 可任意切分，无需与写侧的段边界对齐。
- 0x03/0x04 协商的偏移在两端都按**未压缩的逻辑字节**计数，因此续传、进度与接收限速在压缩装饰器下语义不变。
- 与 0x07 一样，旧版本对端会丢弃 0x08 帧导致连接断开；客户端据此关闭压缩重连一次（最多两次回退：先关压缩，再关加密）。
- **服务器仅在通过认证后才接受 0x08**（配置了配对码时）。否则未认证对端可先堆叠解压装饰器、再按自定义长度触发大额分配，形成预认证内存放大。

### 完成确认（TCP 专用，v2.12+）

0x00 / 0x01 / 0x06 原本在接收端**没有任何正向应答**：TCP 上接收方若拒绝（磁盘不足、接收确认被拒、IP 过滤），发送方照样把数据写进 socket 缓冲并报告"完成"——即静默丢文件。v2.12 起：

- 接收端在处理完 0x00 / 0x01 / 0x06 后，**穿过当前（可能已加压缩/加密装饰器的）流**写回 1 个字节：`0x01` 表示已接受，`0x00` 表示被拒绝。
- 发送端在报告完成前读取该字节；读到 `0x00` 或连接在收到任何字节前关闭 → 抛错，不再误报成功。
- **旧版本互操作**：发送端只有在握手阶段确认对方为当前版本（加密或压缩前导帧被接受）后才等待该字节；否则（无配对且关闭压缩，或对端版本过旧）不等，避免对旧服务器无限等待。UDT 不受影响——它沿用既有的带外 1 字节 ACK，线格式完全不变。
- 该确认字节经装饰器写入，所以压缩/加密会话同样适用（`CompressedWireStream`/`EncryptedWireStream` 的 `Dispose` 不再关闭底层 socket，socket 生命周期归传输层所有）。

### 分块完成判据（类型 `0x02`）

服务端 `ChunkTracker` 以**已接收区间的实际覆盖**判断完成，而不是累加各块大小：区间存入时即合并排序，仅当并集恰好覆盖 `[0, totalSize)` 才算完成。这挡掉了重叠/重复分块把字节总和刷过 `totalSize`、却在文件中留下空洞被误判为「完成」的情况。同名文件若上次中断时声明的大小不同，旧重组状态会被驱逐（否则两次文件的数据会混在一起）。

### 文本消息（类型 `0x06`）

```
[1 byte:  0x06]
[4 bytes: Int32 byteLen]        // UTF-8 字节数，0 ≤ byteLen ≤ 1 MB，超出拒绝
[N bytes: UTF-8 text]
```

送达确认与文件语义一致：TCP 复用上面的**完成确认字节**（`0x01`/`0x00`），UDT 复用既有的 1 字节应用层 ACK。接收端经 `OnTextReceived` 事件上抛给 UI（日志 + 气泡通知 + 非模态文本窗口）。

### 服务器行为

- 文件名通过 `Path.GetFileName()` 清理；文件夹传输的相对路径通过 `SanitizeRelativePath` 处理（替换 `..` / `.`）。
- 名称冲突时在扩展名前追加 `_1`、`_2` 等。
- 任何文件 SHA-256 不匹配，整个文件夹传输中止。
- 分块传输的 `ChunkTracker` 在首块到达时预分配文件，即使后续块乱序到达也能正确写入对应偏移位置。

---

## UDT 协议

UDT（UDP-based Data Transfer）在 UDP 之上提供可靠、有序字节流（STREAM 模式）。拥塞控制、丢包恢复和重排序由 UDT4/libudt v4.11 库内部处理。

**应用层协议与 TCP 完全相同**——UDT 的 `TransferUdtServer` 和 `TransferUdtClient` 发送/接收与 TCP 章节所述相同格式的数据（1 字节类型 + 头部 + 载荷 + SHA-256），仅传输层不同。

### UDT 特性

| 特性 | 说明 |
|------|------|
| 模式 | STREAM（类 TCP 语义） |
| 拥塞控制 | UDT 内置（类似于 TCP BIC） |
| 超时 | `UDT_RCVTIMEO` / `UDT_SNDTIMEO` = 30 秒 |
| 缓冲区 | 4 MB（发送 & 接收） |
| DLL 大小 | `udt.dll` + `libmcfgthread-2.dll` ≈ 实体内嵌 |

### 连接建立

UDT `udt_connect()` 为异步握手（返回时可能仍处于 BOUND 状态）。客户端通过 `WaitForConnectionReady()` 轮询（30 次 × 200ms），执行一次小 `udt_send()` 等待套接字进入 CONNECTED 状态后再发送实际数据。

### 类型支持

UDT 传输支持所有 TCP 线协议类型：
- `0x00` — 单文件
- `0x01` — 文件夹
- `0x02` — 分块文件（配合 `ConcurrentTransfer`）

### 服务端

`TransferUdtServer` 使用 `udt_bind()` → `udt_listen(10)` → accept 循环（类似 TCP 服务器模式）。每个客户端连接通过 `udt_accept()` 获取独立套接字，fire-and-forget `HandleClient`。Stop 时关闭所有活跃客户端套接字以中断阻塞中的 I/O。

### 客户端

`TransferUdtClient` 使用 `udt_connect()` → `WaitForConnectionReady()` → 发送 TCP 线协议数据 → drain 接收 → `udt_close()`。

### DLL 嵌入

`udt.dll` 和 `libmcfgthread-2.dll` 通过 csc.exe `/resource` 嵌入 exe。首次运行时由 `UdtDll.EnsureExtracted()` 提取到 exe 目录（只读目录回退到 `%TEMP%` + `SetDllDirectory`）。杀软文件锁采用重试机制（最多 3 次，间隔 200ms）。P/Invoke 通过 `udt_c_wrapper.cpp` 导出的纯 C 函数（14 个 API）调用，使用 `CallingConvention.Cdecl`。

### 生命周期

`UdtStartup()` / `UdtCleanup()` 通过引用计数管理——仅当计数归零时才调用原生 `udt_cleanup()`，防止客户端传输完成时关闭活跃的服务器连接。

---

## SHA-256 完整性

两种协议都在数据载荷后追加 32 字节文件内容 SHA-256 哈希。
哈希在传输过程中增量计算（流式）。比较使用恒定时间 XOR 循环以防止时序侧信道攻击。
